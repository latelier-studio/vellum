# design.md example

Vellum-native DSL. Warm "Aurora" palette — orange accent, beige neutrals.

Run:
```bash
pnpm vellum tokens preview design.md
```

Or copy this `design.md` into a new project and run `vellum init`.
